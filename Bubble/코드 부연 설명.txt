1. PlayerBase에서 컴포넌트를 map으로 들고 있는 이유는 코드가 간결해지기 때문이다
	만약 enum과 vector를 활용한다면
	해당 컴포넌트에 접근하는데 O(1)이 걸리지만 코드가 복잡해진다
	또한 Actor들 마다 사용하는 컴포넌트가 다르기도 하다

2. PlayerState의 State들은 왜 vector로 관리하는가
	컴포넌트와는 다르게 모든 State들을 전부 사용
	(근데 Map으로 바꿀까 고민중)
